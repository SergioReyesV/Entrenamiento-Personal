import { createSignal } from "solid-js"


export function EjemploRenderingList(){
const [getElement, setElement] = createSignal('');

    return (
        <div>
            hola
        </div>
    );
}